# 健康航程｜第一版互動原型

## 本版界線

這是設計驗證，不是可收案的正式系統。所有預設身分、藥品、時段、步數、徽章均為虛構。
運動紀錄改為私人網站的 D1 資料庫與 R2 原圖留存，依平台注入的登入者 ID 隔離。確認儲存後可重新讀取，不以瀏覽器暫存冒充留存。
飲食、用藥、暱稱、排行榜參與及綁定示範仍只存在 React 記憶體。沒有 LINE OAuth、Messaging API、正式個案／管理員角色或外部訊息傳送。
飲食圖片仍由瀏覽器本機 FileReader 預覽，不上傳。運動截圖在瀏覽器以本站供應的 Tesseract.js 模型辨識，只有確認提交時才上傳私人儲存。截圖維持原始位元組，包含原有中繼資料，不宣稱已去除 EXIF 或完成惡意檔案掃描。僅可使用無真實個資的示範截圖。
CSV 是使用者主動下載的示範摘要，下載後不會隨重新整理清除。
網站發布限定擁有者存取；內頁「管理端」切換不構成真正的權限機制。

## 介面與試用流程

1. 預設進入已綁定的王示範：試填運動、示範餐點及早晚用藥。
2. 查看健康紀錄。每項任務每天只算一次；修改不累加點數。
3. 排行榜預設不參加。開啟後僅顯示暱稱與步數。
4. 我的帳號 → 體驗首次綁定 → 模擬 LINE 授權 → 姓名固定王示範、綁定碼 HV2026 → 確認示範說明 → 申請。
5. 管理端 → 王示範 → 審核綁定 → 確認。核准後可回今日任務。
6. 用藥回報選擇未服用／有疑問後，管理端顯示待關懷。可標記已關懷並新增站內回饋，個案首頁可見。
7. 匯出只包含目前篩選的虛構個案摘要，CSV 有 UTF-8 BOM 與公式注入防護。

## 規則

- 固定示範日期 2026-09-02；不進行跨日排程。週曆未来日期顯示 —。
- 運動、飲食、完整早晚用藥回報各 10 點；今日最多 30 點。
- 「未服用」「有疑問」同樣算真實回報；「稍後再記」不完成任務，也不會發送通知。
- 任務完成率不是服藥遵從率。排名不含用藥或飲食。
- LINE user ID ↔ internal participant ID ↔ 院內病歷号將在正式伺服器隔離儲存；本版不收集身分證字號。

## 運動截圖辨識與留存

### 截圖相容性修正與單一指標呈現

- 改為全圖文字辨識，加上獨立英文數字模型辨識主要數值區域。避免混合語言模型漏讀圓環內大字、數字拆字及步數標題與數值分離的問題。局部辨識只是讀取矩形區域，不修改或覆蓋原圖。
- 使用版面位置、字高與步數頁標題辨識候選值；較小的目標數字、图表刻度與重點區平均值不當成本日步數。無法分辨兩個相近大小的候選值時保持留白。
- 本機以使用者提供的三張實際截圖做相同 OCR 流程回歸測試；原始及處理過的照片均未加入 Git、網站資源或雲端個案資料。並新增使用合成數值的版面單元測試。
- 介面改為「記錄步數／記錄運動時間」擇一，僅顯示、提交及呈現所選指標，另一欄不需補填。辨識值仍是候選值，需要使用者核對當日與日／週／月範圍。
- 另觀察到網站請求缺少平台穩定 user ID 時會回應 401；維持拒絕讀寫，不以 email 或共用 ID 降級替代。新增清楚的本機辨識／雲端儲存狀態與平台重新登入入口。尚未驗證重新登入是否能解決該平台身分問題。

- 預設：選取相簿截圖 → 本機文字辨識 → 確認／修正數值 → 明確同意 → 上傳原圖與紀錄。無法直接讀取其他手機 App。
- 支援 PNG、JPEG、WebP，單張上限 5 MB；辨識模型與 Worker 均從本站載入，截圖不傳外部 AI 或 CDN。Tesseract 6.0.1 配合繁體中文與英文整數模型。
- 以標籤擷取步數與運動分鐘；遇到多個不同值、缺少標籤或無法辨識時留白，不推測手機時鐘、目標或配速。至少填寫一項；時間換算為整分鐘，秒數捨去。
- 原型仍固定 2026-09-02，日期由使用者對照截圖確認，不宣稱已自動核對日期。僅同一天的最新修訂計入一次任務及步數，不累加修訂版本。
- 每次提交新增不可由一般 API 修改的版本，保留原始辨識文字、擷取值、確認值、原因、伺服器時間、前版本 ID 及原圖 SHA-256。編輯既有圖時由伺服器沿用已儲存辨識結果，不接受客戶端改寫原始值。
- 原圖使用隨機儲存鍵，無公開圖片網址；讀取必須由後端驗證登入者為紀錄擁有者。管理端示範仍是同一擁有者的視圖，不是真實臨床管理權限。
- 來源為瀏覽器端 OCR 與個案確認，不是防竄改裝置證明或經審核的客觀步數。未實作防作弊、惡意檔案掃描、正式覆核、資料生命週期及院方備援。
- 儲存採伺服器端驗證、同源提交、檔案大小／檔頭限制、參數化查詢及最新版本比對；同時編輯發生衝突時要求重新開啟，不悄悄覆蓋。上傳失敗不顯示成功。每個登入者最多 100 筆原型修訂。
- 目前未設定自動刪除期間。正式收案前須確認院方儲存位置、保存期限、刪除及備份規則；不可直接沿用本私人示範站收取真實病患資料。
- 參考：[Tesseract.js 官方本機部署文件](https://github.com/naptha/tesseract.js/blob/master/docs/local-installation.md)。

## 正式上線前必備

- 院方管理的 LINE Provider、Login / LIFF 與 Messaging API channel，以及正式 HTTPS callback。
- 伺服器驗證 token 的簽章、issuer、audience、expiry、nonce；CSRF/session 保護。
- 一次性隨機綁定碼，限時、單次使用、錯誤速率限制與人工核對；不可使用本版固定碼。
- 個案、照護者、管理員的獨立權限；後端強制資料隔離、管理員強驗證、稽核。
- 資料庫、私有圖片儲存、加密、圖片格式驗證與 EXIF 去除、惡意檔案防護。
- 處方清單維護、處方異動保留、時段與停藥規則、跨日與補登政策。
- 院方個資告知、合法蒐集依據、研究倫理評估（適用時）、保存期限及權利申請流程。
- LINE 提醒同意、安靜時段、訊息額度與團隊關懷 SOP；不得暗示緊急監測。
- 部署與備份復原測試、資安複查、瀏覽器與手機實機測試。

## 建置環境決策

保留 Vite + React 與 shadcn 介面，新增小型 Cloudflare Worker API。前端輸出 dist/client，後端輸出 dist/server/index.js；資料庫遷移位於 drizzle。依本機既有相容性紀錄，停用自動清空舊輸出，Sites metadata hook 改用非刪除式複製。
發布僅封裝當前 client、server/index.js、hosting metadata 與生成的 migrations；不包含舊 RSC 中間產物、原始碼、測試圖片或真實資料。開發預覽不連正式資料庫，只有部署後可操作私人留存。

## 驗證

領域與後端契約測試：`npm test`，包含 OCR 文字擷取、缺值處理、圖片檔頭、未登入／跨來源／跨擁有者拒絕、保存讀回、修正歷程及舊版衝突。
型別：`npx tsc --noEmit`。建置：`npm run build`。
依 Sites 技能，本輪未進行未經要求的瀏覽器 UI 自動測試；已提供預覽並檢查 HTTP 回應。
實際 OCR smoke test：以 scripts/create-ocr-fixture.ps1 生成無個資的繁中與英文合成截圖，再以 scripts/ocr-smoke.mjs 驗證。已另以使用者提供的三張真實 App 版面附件在本機驗證；未進行手機瀏覽器上傳流程驗收，不保證所有 App 準確率。
WebMCP 為 optional feature-detect，保留讀取摘要並將寫入工具改成開啟截圖表單，不繞過使用者確認。沒有可用的支援上下文時，不宣稱已驗證註冊與執行。
依賴檢查：正式 runtime 套件無已知 npm audit 漏洞；drizzle-kit 的間接開發用 esbuild 存在 4 項 moderate 警示，未使用該套件的網路開發伺服器，且不包入發布 Worker。不能宣稱整個開發依賴鏈零警示。
